# Turkish is the template locale: write new copy into app_tr.arb first, then
# mirror the key into app_en.arb. Keys are always English; only the values
# are translated.
arb-dir: lib/l10n
template-arb-file: app_tr.arb
output-localization-file: app_localizations.dart
output-class: L
nullable-getter: false
