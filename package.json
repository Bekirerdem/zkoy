{
  "name": "zkoy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server/index.ts",
    "start": "bun run src/server/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "qrcode-generator": "^2.0.4"
  }
}
